## Product Requirements Document (PRD)


Given an input image the application should read the numbers from the image.

The input image has the numbers in a table like format.

The input image numbers are hand written numbers.

